---
- name: Converge
  hosts: localhost
  gather_facts: false
  vars: 
  - external_ingress_kubeconfig: "$HOME/.kube/config"
  tasks:
    - name: "Include neatleap.ensure_external_ingress_state"
      ansible.builtin.include_role:
        name: "ansible_role_ensure_external_ingress_state"